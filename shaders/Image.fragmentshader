#version 330 core

in vec2 TexCoords;

uniform sampler2D image;
uniform vec3 spriteColor;
uniform float alpha;

out vec4 color;

void main()
{
    color = vec4(spriteColor, alpha) *  texture(image, TexCoords);
}